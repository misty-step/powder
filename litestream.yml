dbs:
  - path: /data/powder.db
    replicas:
      - type: s3
        bucket: ${BUCKET_NAME}
        path: powder.db
        endpoint: ${AWS_ENDPOINT_URL}
        region: ${AWS_REGION}
        force-path-style: true
        snapshot-interval: 1h
