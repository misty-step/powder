/target/
**/*.rs.bk
.DS_Store
.env
/data/
*.db
*.db-shm
*.db-wal

# powder-951: local `canary integrate` state -- an operator-instance
# artifact (paths, monitor/target ids, canary endpoint), not product source.
# Regenerated by `bin/canary integrate` against whichever machine runs it.
.canary/
