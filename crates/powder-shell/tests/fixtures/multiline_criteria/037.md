# sploot-037: wrapped error shape with code span

Priority: P2 | Status: ready

## Goal
Return structured errors from the thumbnail worker.

## Oracle
- [ ] The worker emits `ThumbnailError::SourceUnavailable` when the upstream blob store returns
    a status code other than `200 OK`, `404 Not Found`, or `410 Gone`.
